const text = `Any delicate you how kindness horrible outlived servants. You high bed wish help call draw side. Girl quit if case mr sing as no have. At none neat am do over will. Agreeable promotion eagernessas 
we resources household to distrusts. Polite do object at passed it is. Small for ask shade water manor think men begin.`;

const lowerText = text.toLowerCase();
const newText = lowerText.replaceAll("you", "u");
console.log(newText);

const sideText = "hello world, hello people!";
const newSideText = sideText.replace("hello", "bye");
console.log(newSideText);
