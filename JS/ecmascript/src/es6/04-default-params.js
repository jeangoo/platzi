function newBotCreated(name, usage) {
    var name = name || 'Robot'
    var usage = usage || 'Reminders'
    console.log(name, usage)
}

newBotCreated()
newBotCreated("Claude", "AI assistant")

function newAiAssistant(name = "RemindersBot", usage = "Reminders") {
    console.log(name, usage)
}

newAiAssistant()
newAiAssistant("Deepseek", "AI assistant")

